<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence
                                 https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd"
             version="3.0">

    <!--
      ==================================================================
       PERSISTENCE UNIT 1: PRODUCTION (MSSQL)
      ==================================================================
       - Sinh viên dùng đơn vị này khi chạy Main class (CRUD trên MSSQL thật).
       - Trước khi chạy: tạo database `HSF301_Chapter1_Ex1` trong SQL Server.
       - Đổi user/password phù hợp với SQL Server cá nhân của bạn.
    -->
    <persistence-unit name="hsf302-chapter1" transaction-type="RESOURCE_LOCAL">
        <class>traltb.fudn.chapter1_exercise1.pojo.Student</class>
        <class>traltb.fudn.chapter1_exercise1.pojo.Book</class>
        <properties>
            <property name="jakarta.persistence.jdbc.driver"
                      value="com.microsoft.sqlserver.jdbc.SQLServerDriver"/>
            <property name="jakarta.persistence.jdbc.url"
                      value="jdbc:sqlserver://localhost:1433;databaseName=HSF301_Chapter1_Ex1;encrypt=true;trustServerCertificate=true"/>
            <property name="jakarta.persistence.jdbc.user" value="sa"/>
            <property name="jakarta.persistence.jdbc.password" value="123"/>
            <property name="hibernate.dialect"
                      value="org.hibernate.dialect.SQLServerDialect"/>
            <property name="hibernate.hbm2ddl.auto" value="update"/>
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
        </properties>
    </persistence-unit>

    <!--
      ==================================================================
       PERSISTENCE UNIT 2: TEST (H2 in-memory)
      ==================================================================
       - Đơn vị này dùng cho `mvn test` và GitHub Actions auto-grading.
       - H2 in-memory: KHÔNG cần cài MSSQL.
       - Mỗi test class chạy với database trống → đảm bảo test isolation.
       - Hibernate auto-create schema dựa trên @Entity annotation.
       - DO NOT MODIFY: nếu sửa đơn vị này, test sẽ fail.
    -->
    <persistence-unit name="hsf302-chapter1-test" transaction-type="RESOURCE_LOCAL">
        <class>traltb.fudn.chapter1_exercise1.pojo.Student</class>
        <class>traltb.fudn.chapter1_exercise1.pojo.Book</class>
        <exclude-unlisted-classes>true</exclude-unlisted-classes>
        <properties>
            <property name="jakarta.persistence.jdbc.driver"
                      value="org.h2.Driver"/>
            <property name="jakarta.persistence.jdbc.url"
                      value="jdbc:h2:mem:hsf302_test;DB_CLOSE_DELAY=-1;MODE=MSSQLServer;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE"/>
            <property name="jakarta.persistence.jdbc.user" value="sa"/>
            <property name="jakarta.persistence.jdbc.password" value=""/>
            <property name="hibernate.dialect"
                      value="org.hibernate.dialect.H2Dialect"/>
            <property name="hibernate.hbm2ddl.auto" value="create-drop"/>
            <property name="hibernate.show_sql" value="false"/>
            <property name="hibernate.format_sql" value="false"/>
        </properties>
    </persistence-unit>

</persistence>
